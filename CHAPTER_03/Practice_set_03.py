# Enter the user entered name just after Good Morning

a=input("Enter the name: ", )
print("Good Morning", a)

# Write a program to detect double space in a string

name = "Dheeraj  is  in  Pune"
print(name.find("  "))

#Replace the double spaces with single spaces

print(name.replace("  "," "))
print(name) #Strings are immutable means it cannot be changed